interface:
  display_name: "H3 尾帧续写提示词"
  short_description: "生成含中英提示词、指定段数和时长的工作流 JSON"
  default_prompt: "Use $h3-tailchain-continuity to create a workflow-ready sequence.json with prompt_en, prompt_cn, my segment count, and per-segment durations without inventing story or scene content."

policy:
  allow_implicit_invocation: true
